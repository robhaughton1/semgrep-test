 rules:
  - id: no-eval # name of the rule being created
    message: "Avoid using eval() — it can lead to code injection vulnerabilities." # what SAST will show when vulnerability is found
    severity: WARNING  # the vulnerability severity
    languages: [javascript] # what language this rule applies to (in this case, javascript)
    pattern: eval($X) # Matches any eval() call



    # The pattern is the main thing that is being used to identify the vulnerabilities.d



